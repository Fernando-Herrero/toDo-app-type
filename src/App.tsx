import { Route, Routes } from "react-router-dom";
import "./App.css";

export const App = () => {
    return (
        <main>
            <Routes>
                <Route path="/" element={} />
                <Route path="*" element={} />
            </Routes>
        </main>
    );
};
